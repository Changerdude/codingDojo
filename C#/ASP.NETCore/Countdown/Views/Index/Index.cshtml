<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Countdown</title>
</head>
<body>
    <div class="container rounded shadow bg-dark text-light p-4 my-3">
        <div class="d-flex justify-content-evenly mb-4">
            <div class="d-flex flex-column align-items-center border border-light p-4 m-4">
                <h1>Start Time</h1>
                @{
                    DateTime CurrentTime = DateTime.Now;
                    <h2>@CurrentTime.ToString("MMMM dd, yyyy")</h2>
                    <h3>@CurrentTime.ToString("hh:mm tt")</h3>
                }
            </div>
            <div class="d-flex flex-column align-items-center border border-light p-4 m-4">
                <h1>End Time</h1>
                @{
                    var EndDate = new DateTime(2023, 4, 27, 8, 00, 00);
                    <h2>@EndDate.ToString("MMMM dd, yyyy")</h2>
                    <h3>@EndDate.ToString("hh:mm tt")</h3>
                }
            </div>
        </div>
        <div class="text-center  p-4 m-4">
            <h1>Time Remaining</h1>
            @{
                System.TimeSpan TimeDifference = EndDate.Subtract(CurrentTime);
                <h3>@TimeDifference.Days.ToString() Day(s), @TimeDifference.Hours.ToString(), Hour(s), @TimeDifference.Minutes.ToString() Minute(s)</h3>
                <h3>Remaining</h3>
            }
        </div>
    </div>
</body>
</html>